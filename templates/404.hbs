<!DOCTYPE html>
<html>
    <head>
        <link type="text/css" rel="stylesheet" href="./css/styles.css">
    </head>
    <body>
        {{>header}}
        {{errorMessage}}
        {{>footer}}
    </body>
</html>
